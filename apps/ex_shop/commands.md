```
pikender@Pikenders-MacBook-Pro:~/elixir$ mix phoenix.new ex-shop
** (Mix) Application name must start with a letter and have only lowercase letters, numbers and underscore, got: "ex-shop". The application name is inferred from the path, if you'd like to explicitly name the application then use the `--app APP` option.
pikender@Pikenders-MacBook-Pro:~/elixir$ mix phoenix.new ex-shop --app ex_shop
```

```
npm install && node node_modules/brunch/bin/brunch build
```
